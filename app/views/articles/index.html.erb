<h1>Articles</h1>

<% @articles.each do |article| %>
    <h2><%= article.title %></h2>
    <h4><%= article.desc %></h4>
    <a href="<%= article_path(article.id) %>" class="btn btn-primary" >See article</a>
<% end %>

